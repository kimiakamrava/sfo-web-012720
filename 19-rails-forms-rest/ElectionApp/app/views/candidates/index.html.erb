<h1>2020 Election</h1>

<h2>Candidates</h2>
  <ul>
    <% @candidates.each do |candidate| %>
      <li><%= link_to("#{candidate.name}, #{candidate.party}", candidate_path(candidate)) %></li>
    <% end %>
  <ul>
